= flash[:error]
.container
  = image_tag asset_path("toppage.jpg"), class: "top_page"
  .mask
  .search_container
    %h1 Search for an Artist
    = form_with(url: search_songs_path, method: :get, local: :true) do |f|
      = f.search_field :name_cont,placeholder: "artist name      e.g)  ariana,  maroon"
      %input#search.btn.btn-primary{type: "submit", value: "Search"}